//samo proprety
import Repos from "./repoService"
import Users from "./userService"

class Things {
    constructor(name,url) {
        this.name = name;
        this.url = url;
    }
}

export default Things;